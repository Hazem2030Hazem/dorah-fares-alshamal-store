# 💳 دليل تفعيل بوابات الدفع — خطوة خطوة
## شركة درة فارس الشمال للتجارة

> نفّذ الخطوات بالترتيب. كل خطوة مستقلة — لو وقفت في نصها كملها بعدين.

---

## الخطوة 1️⃣ — تنفيذ SQL (دقيقة واحدة)
1. افتح **Supabase Dashboard** → مشروعك → **SQL Editor**
2. الصق محتوى ملف `بوابات-الدفع.sql` كاملاً → **Run**
3. المفروض تشوف: **Success** ✅

ده بيعمل جدول `payment_gateways` (فيه البوابات الثلاث معطّلة) + سياسات الأمان + View عامة للموقع.

---

## الخطوة 2️⃣ — إنشاء حساب الأدمن الآمن (دقيقتان)
> ضروري جداً — بدونه مش هتقدر تفتح تبويب بوابات الدفع.

1. من Supabase Dashboard → **Authentication** → **Users**
2. دوس **Add user** ← **Create new user**
3. اكتب:
   - **Email**: إيميلك الرسمي (مثلاً mohamed.ali@df-alshamal.com)
   - **Password**: باسورد قوية تحفظها كويس (12+ حرف، أرقام وحروف)
4. فعّل ✅ **Auto Confirm User** (مهم — عشان ميستناش تأكيد إيميل)
5. دوس **Create user** ✅

---

## الخطوة 3️⃣ — نشر الـ Edge Function (5 دقائق)
> ده "السيرفر الصغير" اللي بيكلم البوابة بمفاتيحك السرية بأمان.

1. من Supabase Dashboard → **Edge Functions** (من القائمة الجانبية)
2. دوس **Create a new function** (أو Deploy via dashboard)
3. اسم الفانكشن: `create-payment` (بالظبط كده — حروف صغيرة)
4. امسح الكود الافتراضي والصق كود ملف:
   `supabase/functions/create-payment/index.ts` (موجود في حزمة الملفات)
5. دوس **Deploy** ✅
6. **مهم:** لو الفانكشن طلبت JWT verification — اقفلها من إعدادات الفانكشن
   (Function settings ← Verify JWT with legacy secret ← **OFF**) لأن العملاء بيدفعوا من غير تسجيل دخول

---

## الخطوة 4️⃣ — رفع ملفات الموقع على GitHub
ارفع الملفات دي (الموجودة في الحزمة):
- `admin.html` + `admin-v2.js` + `admin-v2.css` (اللوحة المحدثة)
- `checkout.html` (تدفق الدفع الإلكتروني)
- `test-payment.html` (محاكي الدفع التجريبي)
- `payment-return.html` (صفحة العودة بعد الدفع)

**ملف الـ index.ts مش بيترفع على GitHub** — مكانه Supabase (الخطوة 3).

---

## الخطوة 5️⃣ — التجربة بوضع Test 🧪 (بدون فلوس حقيقية)
1. افتح `admin.html` → سجل دخول **بالإيميل والباسورد** اللي عملتهم في الخطوة 2
2. ادخل تبويب **💳 بوابات الدفع** → اختار **Moyasar** → **إعدادات**
3. حط أي قيم تجريبية: Publishable = `pk_test_demo`, Secret = `sk_test_demo`
4. الوضع: **test** ← فعّل البوابة ✅ ← حفظ
5. افتح الموقع كعميل → ضيف منتج للسلة → checkout
6. هتلاقي خيار جديد: **💳 دفع إلكتروني (Moyasar 🧪)**
7. اكمل الطلب ← هيحولك لصفحة المحاكي ← دوس **محاكاة نجاح الدفع**
8. هترجع لصفحة "تم الدفع بنجاح 🎉" والطلب يتسجل مدفوع ✅

---

## الخطوة 6️⃣ — التفعيل الحقيقي Live 🔴 (لما تجيب حساب البوابة)
1. سجل في **moyasar.com** (الأسهل سعودياً) ← اكمل التوثيق التجاري
2. من لوحة Moyasar خد: **Publishable Key** و **Secret Key** (الحقيقيين مش test)
3. افتح تبويب بوابات الدفع في لوحتك ← Moyasar ← إعدادات
4. حط المفتاحين الحقيقيين ← حوّل الوضع لـ **live** ← اقرأ التحذير ← حفظ
5. خلاص — العملاء بقوا يدفعوا مدى/فيزا/Apple Pay والفلوس توصل حسابك 🎉

> نفس الخطوات تنطبق على **Tap** (tap.company) و **HyperPay** لو حبيت تفعلهم.

---

## ⚠️ ملاحظات أمان مهمة:
- المفتاح السري **مش بيظهر في المتصفح أبداً** — حتى في لوحة الإدارة بيظهر ●●●●
- بعد ترقية الدخول، **كلمة السر القديمة لسه شغالة مؤقتاً** — بعد ما تتأكد إن الإيميل شغال، قولّي عشان نقفلها نهائياً
- لو نسيت باسورد الأدمن: Supabase → Authentication → Users → تقدر تعمل Reset

---

## 🆘 لو حاجة مشتغلتش:
| المشكلة | الحل |
|---|---|
| "بوابات الدفع تتطلب الدخول بحساب الأدمن" | نفّذ الخطوة 2 وسجل دخول بالإيميل |
| "الدفع الإلكتروني غير متاح حالياً" | نفّذ الخطوة 3 (الفانكشن) وتأكد من اسمها |
| الفانكشن ترجع 401 | اقفل JWT verification (الخطوة 3 نقطة 6) |
| خيار الدفع الإلكتروني مش ظاهر | تأكد إن البوابة is_active=true من اللوحة |
